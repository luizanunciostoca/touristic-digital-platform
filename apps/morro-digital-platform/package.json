{
  "name": "@touristic/morro-digital-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@touristic/core": "workspace:*",
    "@touristic/geospatial": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm -w build && node tooling/dev-server.mjs",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
