{
  "name": "touristic-digital-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test && vitest run tests/assistant-v1-baseline.test.ts",
    "typecheck": "turbo run typecheck",
    "architecture:check": "node tooling/architecture/check-dependencies.mjs && pnpm platform:contracts:check",
    "features:check": "node -e \"const r=require('./docs/features/registry.json'); const ids=r.features.map(f=>f.id); if(new Set(ids).size!==ids.length) process.exit(1); if(r.features.some(f=>!/^FEATURE-\\d{4}$/.test(f.id))) process.exit(1); console.log('Feature Registry válido:', ids.length, 'features')\"",
    "platform:contracts:check": "node tooling/quality/check-platform-contracts.mjs",
    "environment:check": "node tooling/quality/check-environment-reconciliation.mjs",
    "design-system:inventory": "node tooling/design-system/inventory-v1-css.mjs",
    "affiliates:lint": "eslint packages/affiliates/src",
    "affiliates:typecheck": "tsc -p packages/affiliates/tsconfig.json --noEmit",
    "affiliates:test": "vitest run packages/affiliates/src/index.test.ts",
    "affiliates:build": "tsc -p packages/affiliates/tsconfig.json",
    "affiliates:server:lint": "pnpm --filter @touristic/affiliates-server lint",
    "affiliates:server:typecheck": "pnpm --filter @touristic/affiliates-server typecheck",
    "affiliates:server:test": "pnpm --filter @touristic/affiliates-server test",
    "affiliates:server:build": "pnpm --filter @touristic/affiliates-server build",
    "affiliates:check": "pnpm affiliates:lint && pnpm affiliates:typecheck && pnpm affiliates:test && pnpm affiliates:build && pnpm affiliates:server:lint && pnpm affiliates:server:typecheck && pnpm affiliates:server:test && pnpm affiliates:server:build",
    "check": "pnpm format:check && pnpm architecture:check && pnpm features:check && pnpm environment:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm affiliates:check",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "release:gates": "node tooling/quality/run-local-release-gates.mjs",
    "ticketing:browser:smoke": "node --test tooling/ticketing/browser-contract.test.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@types/node": "^24.2.1",
    "eslint": "^9.32.0",
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.39.0",
    "vitest": "^3.2.4"
  }
}
