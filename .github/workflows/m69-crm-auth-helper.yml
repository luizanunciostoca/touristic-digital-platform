name: M69 CRM authorization helper

on:
  workflow_dispatch:
  push:
    branches:
      - feat/crm-m69-authorization-policy
    paths:
      - ".github/workflows/m69-crm-auth-helper.yml"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/crm-m69-authorization-policy
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Integrate workspace lockfile
        run: pnpm install --lockfile-only
      - name: Install frozen workspace
        run: pnpm install --frozen-lockfile
      - name: Format M69 artifacts
        run: pnpm exec prettier --write packages/crm/package.json packages/crm/src/authorization.ts packages/crm/src/authorization.test.ts pnpm-lock.yaml
      - name: Validate CRM package
        run: |
          pnpm --filter @touristic/crm lint
          pnpm --filter @touristic/crm typecheck
          pnpm --filter @touristic/crm test
          pnpm --filter @touristic/crm build
      - name: Validate repository quality
        run: |
          pnpm format:check
          pnpm architecture:check
          pnpm features:check
          pnpm lint
          pnpm typecheck
          pnpm test
          pnpm build
      - name: Commit permanent M69 integration only
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/crm/package.json packages/crm/src/authorization.ts packages/crm/src/authorization.test.ts pnpm-lock.yaml
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "chore(crm): integrate M69 authorization policy"
          git push origin HEAD:feat/crm-m69-authorization-policy
