name: Assistant M9 format

on:
  push:
    branches: [feat/assistant-runtime-m9]

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/assistant-runtime-m9
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write packages/assistant/src/proactive-suggestions.ts packages/assistant/src/proactive-suggestions.test.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            echo "Formatting already clean"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/assistant/src/proactive-suggestions.ts packages/assistant/src/proactive-suggestions.test.ts
          git commit -m "style(assistant): format M9 proactive files"
          git pull --rebase origin feat/assistant-runtime-m9
          git push origin HEAD:feat/assistant-runtime-m9
