name: Format NAV-20 Temp

on:
  push:
    branches:
      - feat/navigation-arrival-lifecycle
    paths:
      - .github/workflows/navigation-nav20-format-temp.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/navigation-arrival-lifecycle
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write packages/navigation/src/arrival.ts packages/navigation/src/arrival.test.ts
      - name: Commit formatter output
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/navigation/src/arrival.ts packages/navigation/src/arrival.test.ts
          git commit -m "style: format arrival lifecycle"
          git push origin HEAD:feat/navigation-arrival-lifecycle
