name: Morro Fabric Preflight

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: fabric-preflight-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"
  PNPM_VERSION: "10.15.0"
  TURBO_TELEMETRY_DISABLED: "1"
  TURBO_CACHE_DIR: /tmp/turbo-cache

jobs:
  plan:
    name: fabric / plan
    runs-on: ubuntu-latest
    outputs:
      risk: ${{ steps.plan.outputs.risk }}
      ui: ${{ steps.plan.outputs.ui }}
      auth: ${{ steps.plan.outputs.auth }}
      payments: ${{ steps.plan.outputs.payments }}
      ticketing: ${{ steps.plan.outputs.ticketing }}
      ci: ${{ steps.plan.outputs.ci }}
      docs_only: ${{ steps.plan.outputs.docs_only }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
      - id: changeset
        name: Resolve registered ChangeSet
        env:
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref || github.ref_name }}
        run: |
          path="$(node tooling/fabric/resolve-changeset.mjs "$HEAD_BRANCH" --optional)"
          echo "path=$path" >> "$GITHUB_OUTPUT"
      - id: plan
        name: Build semantic change plan
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          CHANGESET_PATH: ${{ steps.changeset.outputs.path }}
        run: |
          set -euo pipefail
          node tooling/fabric/validate-registry.mjs
          node tooling/fabric/check-external-gates.mjs
          args=(--base "$BASE_SHA" --head "$HEAD_SHA" --json fabric-plan.json)
          if [ -n "$CHANGESET_PATH" ]; then
            args+=(--manifest "$CHANGESET_PATH")
          fi
          node tooling/fabric/plan-change.mjs "${args[@]}"
      - name: Build exact-HEAD proof bundle
        if: ${{ steps.changeset.outputs.path != '' }}
        run: |
          node tooling/fabric/build-proof-bundle.mjs "${{ steps.changeset.outputs.path }}" fabric-proof-bundle.json
      - uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08 # v4.6.0
        with:
          name: fabric-plan-${{ github.run_id }}
          path: fabric-plan.json
          retention-days: 14
      - uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08 # v4.6.0
        if: ${{ steps.changeset.outputs.path != '' }}
        with:
          name: fabric-proof-${{ github.run_id }}
          path: fabric-proof-bundle.json
          retention-days: 30

  fast-proof:
    name: fabric / fast proof
    needs: plan
    if: ${{ needs.plan.outputs.docs_only != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
      - name: Fabric-specific static proof
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        shell: bash
        run: |
          set -euo pipefail
          node tooling/fabric/validate-registry.mjs
          node tooling/fabric/check-external-gates.mjs
          node tooling/fabric/check-change-size.mjs "$BASE_SHA"

  critical-policy:
    name: fabric / critical policy
    needs: plan
    if: ${{ needs.plan.outputs.risk == 'critical' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile --prefer-offline
      - name: Critical invariants
        run: |
          pnpm fabric:registry:check
          pnpm fabric:external-gates:check
          pnpm architecture:check
          pnpm release:readiness:check
          pnpm ci:governance:check
          pnpm ci:supply-chain:strict
