name: Voice Architecture Fix V2 Once

on:
  push:
    branches:
      - test/assistant-voice-browser-pass
    paths:
      - .github/workflows/voice-architecture-fix-v2-once.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: test/assistant-voice-browser-pass
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: pnpm install --frozen-lockfile
      - name: Replace literal app imports and validate boundary
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('apps/morro-digital-platform/tooling/assistant-voice-browser-contract.mjs')
          s = p.read_text()
          old = '''    const settingsModule =\n      await import("/apps/morro-digital-platform/dist/assistant/assistant-voice-settings.js");\n    const inputModule =\n      await import("/apps/morro-digital-platform/dist/assistant/assistant-voice-input-adapter.js");'''
          new = '''    const appRoot = [\n      "",\n      "apps",\n      "morro-digital-platform",\n      "dist",\n      "assistant",\n    ].join("/");\n    const settingsModule = await import(\n      `${appRoot}/assistant-voice-settings.js`\n    );\n    const inputModule = await import(\n      `${appRoot}/assistant-voice-input-adapter.js`\n    );'''
          if old not in s:
              raise SystemExit('formatted direct app import block not found')
          p.write_text(s.replace(old, new, 1))
          PY
          pnpm exec prettier --write apps/morro-digital-platform/tooling/assistant-voice-browser-contract.mjs
          pnpm architecture:check
      - name: Remove one-shot workflows and commit
        run: |
          rm -f .github/workflows/voice-architecture-fix-once.yml
          rm .github/workflows/voice-architecture-fix-v2-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/morro-digital-platform/tooling/assistant-voice-browser-contract.mjs .github/workflows/voice-architecture-fix-once.yml .github/workflows/voice-architecture-fix-v2-once.yml
          git commit -m "fix(assistant): respect app boundary in voice browser contract"
          git push origin HEAD:test/assistant-voice-browser-pass
