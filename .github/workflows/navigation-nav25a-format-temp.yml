name: Format NAV-25A Temp

on:
  push:
    branches:
      - feat/navigation-event-browser-integration
    paths:
      - .github/workflows/navigation-nav25a-format-temp.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/navigation-event-browser-integration
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write apps/morro-digital-platform/src/navigation/navigation-dom-events.ts
      - name: Commit formatter output
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/morro-digital-platform/src/navigation/navigation-dom-events.ts
          git commit -m "style: format NAV-25A DOM event bridge"
          git push origin HEAD:feat/navigation-event-browser-integration
