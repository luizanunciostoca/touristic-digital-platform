name: Assistant M20 format

on:
  push:
    branches: [feat/assistant-m20-domain-handlers]

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/assistant-m20-domain-handlers
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write packages/assistant/src/domain-handlers.ts packages/assistant/src/domain-handlers.test.ts packages/assistant/src/index.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/assistant/src/domain-handlers.ts packages/assistant/src/domain-handlers.test.ts packages/assistant/src/index.ts
          git commit -m "style(assistant): format M20 files"
          git pull --rebase origin feat/assistant-m20-domain-handlers
          git push origin HEAD:feat/assistant-m20-domain-handlers
