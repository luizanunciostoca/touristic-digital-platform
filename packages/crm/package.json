{
  "name": "@touristic/crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@touristic/auth": "workspace:*"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./authorization": {
      "types": "./src/authorization.ts",
      "default": "./dist/authorization.js"
    },
    "./leads-boundary": {
      "types": "./src/leads-boundary.ts",
      "default": "./dist/leads-boundary.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
