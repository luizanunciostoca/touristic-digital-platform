# CRM M78 route vocabulary

Internal authenticated routes use `/api/crm/proposals`, `/api/crm/proposals/accepted`, `/api/crm/proposals/:id/send` and `/api/crm/proposals/:id/respond`. Public tokenized proposal access remains outside M78.
