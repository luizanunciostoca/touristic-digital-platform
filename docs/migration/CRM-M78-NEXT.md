# CRM M78 next-step boundary

M78 closes authenticated HTTP/Node composition for Proposals only.

The next CRM milestone must be selected from the official migration dependency graph after M78 is merged and post-merge gates are healthy. Do not mix public token views, Contracts, browser CRM surfaces, historical migration, storage or AI adapters into M78.
